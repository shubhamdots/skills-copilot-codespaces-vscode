// Create web server
// Create a web server that listens on port 3000 and returns a list of comments in JSON format. The comments are stored in the comments.json file.

// The comments.json file contains an array of comment objects. Each comment object has the following properties:

// id: a unique identifier for the comment (a number)